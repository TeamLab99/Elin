public enum EUILayer
{
    Base,
    UI,
    Setting
};

public enum Scene
{
    Unknown,
    DeckCreator
}

public enum Sound
{
    Bgm,
    Effect,
    MaxCount,
}

public enum UIEvent
{
    Click,
    Drag,

}
public enum MouseEvent
{
    Press,
    Click,
}

public enum CameraMode
{
    QuarterView
}

public enum Sorting
{
    Default,
    Fire,
    Water,
    Wind,
    Earth
}

public enum ItemType
{
    Consumer,
    Equipment,
    Quest,
    Etc
}

public enum Gems
{
    none,
    FireGem,
    WaterGem,
    WindGem,
    EarthGem
}